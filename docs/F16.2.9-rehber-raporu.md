# F16.2.9 — Rehberler İçerik Doldurma Raporu

> **Kanıt temesi:** V1 mimari karar = 4 statik `rehber-*.php` korunur (DB'ye alma V2). Migration `000042` ile `seo_verileri`'ne 24 satır (4 path × 6 dil) yazıldı; idempotence + down/up döngüsü kanıtlandı; frontend 4 sayfada `$SEO_HAM` + router 2 yeni route + sitemap 24 URL + 6 dil × 21 lang key; `SeoService::bantKontrol` byte (strlen) fixi. HTML 4×6=24/24 200 + band PASS; `/seo/check` key checks 24/24 PASS. Ürün kararları geliştiriciye aittir; bu rapor yalnızca yürütme kanıtıdır.
>
> **Tarih:** 2026-09-24 · **Faz:** F16.2 (`işleniyor`) · **Kapsam:** F16.2.9 Rehberler — mimari karar + SEO seed + route/sitemap/lang · **Git:** commit + push (§13)

---

## 1. Özet

F16.2.9 tamamlandı. **Mimari karar (L5 kilitli):** V1'de 4 statik `rehber-*.php` sayfası korunur; içerik/gövde DB'ye alınmaz (V2). Bu turda yalnız **meta katman** (title/desc/canonical/hreflang) DB'ye taşındı: migration `2026_09_24_000042_rehber_seo_seed.php` → `seo_verileri` **24 satır**. Frontend 4 dosyada `$SEO_HAM` pattern (sss/garanti ile aynı) uygulandı; router'a `/rehberler/fark` + `/rehberler/istanbul-bakim-takvimi` route'ları eklendi (öncesi 404); sitemap rehber URL sayısı 12→**24**; 6 dil dosyasına 21 eksik key eklendi (73→**94**). Keşif: İstanbul sayfasında `strict_types` + `str_pad(int)` fatal bug (satır 152) düzeltildi. `SeoService::bantKontrol` `mb_strlen`→`strlen` (AI_CAĞI §7 byte bantı) düzeltildi. Döngü up/up(0)/down/up PASS; DB 24/24 band OK; HTML 24/24 PASS; seo/check 24/24 key PASS.

---

## 2. Kapsam ve Kararlar

| Karar | Kaynak | Uygulama |
|-------|--------|----------|
| **V1: 4 statik sayfa korunur** | Görev mimari karar başlığı (statik vs DB) | Gövde/markup **değişmedi** (yalnız meta); DB'ye alma V2 |
| `sayfa_kodu` = path-style | `SeoService::sayfaCoz` `trim($yol,'/')` | `rehberler/bakim`, `rehberler/malzeme-karsilastirma`, `rehberler/fark`, `rehberler/istanbul-bakim-takvimi` |
| Seed = 24 satır (4×6) | F16.2.9 ADIM | migration 000042, `ON DUPLICATE KEY`, `robots="index, follow"` |
| canonical dil bazlı | hreflang deseni | `https://kamelya.com/[dil/]path` |
| hreflang_json 7 giriş | seo.php `seoMeta` | 6 dil + `x-default` |
| Frontend `$SEO_HAM` | `sss.php`/`garanti.php` pattern | 4 dosya; `$SEO['yol']=$MEVCUT_YOL` |
| Router +2 route | ADIM-3 | match + `$bolumler` + `$bolumAnahtar`/`$ozellikMap` **null** (fail-open; `bakim_takvimi=0` → 410 engeli) |
| Lang +21 key × 6 | eksik key keşfi | `fark_*`, `istanbul_*`, `sonraki_bakim`, `not_*`, `rehberler_baslik`… → 94 key/dil |
| `bantKontrol` byte | AI_CAĞI §7 · `standards/seo/AI_CAĞI_SEO_STANDARDLARI.md` | `strlen` (mb_strlen değil); etiket `N byte (bant X-Y)` |
| **KAPSAM DIŞI** | görev | gövde/markup değişikliği, yeni sayfa/tablo, `demo_icerik`, feature gate değişikliği |

### 2.1 SEO metinleri (byte — frontend ↔ migration eşit 24/24)

| Path | tr T/D | en T/D | de T/D | fr T/D | it T/D | ar T/D |
|------|--------|--------|--------|--------|--------|--------|
| rehberler/bakim | 54/154 | 55/156 | 50/151 | 51/157 | 53/154 | 53/159 |
| rehberler/malzeme-karsilastirma | 53/155 | 56/152 | 55/157 | 54/160 | 58/157 | 60/151 |
| rehberler/fark | 60/154 | 60/153 | 52/158 | 55/158 | 52/154 | 50/159 |
| rehberler/istanbul-bakim-takvimi | 58/151 | 54/155 | 52/154 | 58/158 | 59/156 | 50/153 |

Bant: title 50–60 byte, desc 150–160 byte (UTF-8). 24/24 OK.

---

## 3. ADIM 1 — Mevcut durum (öncesi, keşif)

```
route /rehberler/fark → 404
route /rehberler/istanbul-bakim-takvimi → 404
sitemap grev "rehber" = 12  (yalnız bakim + malzeme × 6 dil)
seo_verileri WHERE sayfa_kodu LIKE 'rehberler/%' = 0
frontend lang tr = 73 key  (eksik: fark_*, istanbul_*, sonraki_bakim, not_*, …)
/bantKontrol mb_strlen → karakter sayıyordu (byte değil)
feature: bakim_rehberi=1, malzeme_rehberi=1, bakim_takvimi=0, rehberler=1
```

---

## 4. ADIM 2 — Migration (döngü)

| Madde | Kanıt |
|-------|-------|
| Dosya | `backend/database/migrations/2026_09_24_000042_rehber_seo_seed.php` |
| `php -l` | `No syntax errors detected` ✓ |
| Nested txn | Yok — `migrate.php` sarmalar |
| Idempotent | `ON DUPLICATE KEY UPDATE` (sayfa_tipi, sayfa_kodu, dil_kodu) |
| `down()` | `DELETE … sayfa_tipi='statik' AND sayfa_kodu LIKE 'rehberler/%'` |

### 4.1 down/up döngüsü

```
=== durum ===
BEKLIYOR  2026_09_24_000042_rehber_seo_seed.php
=== UP ===
Uygulandı: 2026_09_24_000042_rehber_seo_seed.php
Tamam: 1 migration uygulandı.
=== UP2 (idempotent) ===
Tamam: 0 migration uygulandı.
=== DOWN ===
Geri alındı: 2026_09_24_000042_rehber_seo_seed.php
=== DB after down ===
0
=== UP3 ===
Uygulandı: 2026_09_24_000042_rehber_seo_seed.php
Tamam: 1 migration uygulandı.
=== DB final ===
24   (rehberler/%)
6    (sss — dokunulmadı)
=== gocler (son 3) ===
2026_09_24_000042_rehber_seo_seed.php
2026_09_24_000041_garanti_seed.php
2026_09_23_000040_sss_seed.php
```

---

## 5. ADIM 3 — DB doğrulama

```
COUNT rehberler/% = 24
LENGTH(meta_baslik) ∈ [50,60]  → 24/24 OK
LENGTH(meta_aciklama) ∈ [150,160] → 24/24 OK
hreflang_json JSON_LENGTH = 7  (örn. rehberler/bakim × 6 dil) ✓
canonical_url dolu ✓
seo_verileri toplam = kategori 72 + urun 72 + sss 6 + rehber 24 = 174
```

Ornek satır (TR bakim): title 54B, desc 154B, canonical `https://kamelya.com/rehberler/bakim`.

---

## 6. ADIM 4 — Frontend $SEO_HAM (4 dosya)

| Dosya | `$SEO_HAM` | `$SEO` | php -l |
|-------|-----------|--------|--------|
| `rehber-bakim.php` | satır 19–44 | 47–51 | ✓ |
| `rehber-malzeme.php` | satır 18–… | 1 blok | ✓ |
| `rehber-fark.php` | satır 36–… | 1 blok | ✓ |
| `rehber-istanbul-bakim-takvimi.php` | satır 112–137 | 140–144 | ✓ |

- Pattern: `$seoHam = $SEO_HAM[$dil] ?? $SEO_HAM['tr']; $SEO = ['baslik'=>…, 'aciklama'=>…, 'yol'=>$MEVCUT_YOL];`
- Çift `$SEO` bloğu riski temizlendi (fark/malzeme) — her dosyada **tam 1** `$SEO = [` (grep teyidi).
- Frontend ↔ migration metin eşitliği: **24/24** (Python script OVERALL PASS).

---

## 7. ADIM 5 — Router + sitemap

| Değişiklik | Satır (yaklaşık) | Kanıt |
|------------|------------------|-------|
| match +2 | `router.php:61-62` | `/rehberler/fark`, `/rehberler/istanbul-bakim-takvimi` → 200 (öncesi 404) |
| `$bolumler` +2 path | satır 168 | sitemap 12→24 |
| `$bolumAnahtar` null | satır 174 | `bakim_takvimi=0` maplenirse 410 verirdi; null = fail-open |
| `$ozellikMap` null | satır 233 | `'rehber-fark'=>null`, `'rehber-istanbul-bakim-takvimi'=>null` |

```
$ C -s /sitemap.xml | grep -c rehber → 24
```

---

## 8. ADIM 6 — Lang key (6 dil × 21)

Eksik key'ler eklendi: `fark_baslik, fark_giris, fark_tur, fark_yapi, fark_kullanim, fark_maliyet, fark_alan, fark_izin, istanbul_bakim_baslik, istanbul_bakim_giris, sonraki_bakim, sayac_aciklama, aylik_takvim, ay, islem, rehberler_baslik, notlar_baslik, not_1..not_4`.

```
tr=94 en=94 de=94 fr=94 it=94 ar=94
```

Ham key sızıntısı: `/rehberler/fark` ve `/rehberler/istanbul-bakim-takvimi` HTML'inde `fark_baslik|sonraki_bakim|not_1` → **0** (çevrilmiş metin render).

---

## 9. ADIM 7 — SeoService byte fix + HTML/seo/check

### 9.1 `bantKontrol`

```php
// AI_CAĞI SEO §7: UTF-8 byte bandı (mb_strlen değil).
$uzunluk = strlen($deger);
// deger: $uzunluk . ' byte (bant ' . $min . '-' . $maks . ')'
```

### 9.2 HTML 4 path × 6 dil (24/24)

```
HTML PASS 24 FAIL 0 (of 24)  — 200 · title 50–60B · desc 150–160B · fatal=False
```

### 9.3 `GET /api/v1/seo/check` key checks (title+desc+canonical+hreflang)

```
PASS 24 FAIL 0 (of 24)
örnek istanbul tr score=80:
  title PASS 58 byte (bant 50-60)
  meta_description PASS 151 byte (bant 150-160)
  canonical PASS https://kamelya.com/rehberler/istanbul-bakim-takvimi
  hreflang PASS 7
  kapak_resmi FAIL   ← bu sayfalarda og:image yok (bilinçli; kapsam dışı)
```

Not: `sss` tr örneğinde title/desc PASS (55/158), canonical/hreflang FAIL — SSS satırında alanlar NULL (F16.2.3 kapsamı; bu görevde dokunulmadı).

---

## 10. Keşif / bug fix (bu tur)

| Bulgu | Kanıt | Düzeltme |
|-------|-------|----------|
| `/rehberler/istanbul-bakim-takvimi` → title/desc yok, gövdede fatal | curl: `str_pad(): Argument #1 must be of type string, int given` satır 152 (`strict_types=1`, `$ay`/`min(...)` int) | `str_pad((string)$ay, …)`, `str_pad((string)min($gun+7,28), …)` → 200 + T=58 D=151, fatal=False |
| `bantKontrol` karakter → byte | AI_CAĞI §7 | `strlen` + etiket `N byte` |
| Çift `$SEO` bloğu riski (fark/malzeme) | grep `$SEO = [` sayısı | temizlendi; her dosya 1 blok |

---

## 11. Audit (manuel L1 + L2 + SEO)

| Katman | Sonuç | Kanıt |
|--------|-------|-------|
| **L1** (katman disiplini, migration pattern, down cycle, idempotence, php -l, route eşleşmesi) | **PASS** | §4 döngüsü · §5 DB · §6 php -l · §7 route 200 · §3 404 öncesi |
| **L2** (her iddia mysql/curl/php çıktısı; uydurma yok) | **PASS** | §3–§10 ham çıktılar · frontend↔migration 24/24 · raporda sır yok |
| **SEO** | **PASS** (bu modül) | §2.1 band 24/24 · §9 HTML 24/24 · seo/check 24/24 key · sitemap 24 · lang leak 0 · byte fix |

Not: Subagent `/denetle` (phase-auditor + hallucination-guard) bu turda çalıştırılmadı — görev ADIM-8 manuel L1+L2+SEO yazıyor. F16.2.11 kapanışta tam `/denetle` + seo-analyzer zorunlu (F16.2.8 ile aynı prensip).

---

## 12. YAPILACAKLAR + durum dokümanı

- F16.2.9 satırı (~1375): `işleniyor` → **`tamamlandı`**
- Keşifler tablosuna `[EK-20260924]` F16.2.9 satırı eklendi.
- `docs/F16.2-durum-ve-eksikler.md` §1 + §2 rehber satırı güncellendi.

---

## 13. Dosya listesi (commit)

```
M YAPILACAKLAR.md
M backend/app/Services/SeoService.php          (bantKontrol strlen)
?? backend/database/migrations/2026_09_24_000042_rehber_seo_seed.php
M docs/F16.2-durum-ve-eksikler.md
?? docs/F16.2.9-rehber-raporu.md
M frontend/lang/{ar,de,en,fr,it,tr}.php        (+21 key × 6)
M frontend/router.php                          (+2 route, sitemap, ozellik null)
M frontend/sayfalar/rehber-bakim.php           ($SEO_HAM)
M frontend/sayfalar/rehber-fark.php            ($SEO_HAM)
M frontend/sayfalar/rehber-istanbul-bakim-takvimi.php  ($SEO_HAM + str_pad fix)
M frontend/sayfalar/rehber-malzeme.php         ($SEO_HAM)
```

**Commit mesajı:** `F16.2.9: Rehber mimari karar (V1 statik) + 24 seo_verileri + 2 route + sitemap 24 + lang keys + SeoService byte fix + rapor`

---

## 14. Git durumu (rapor anı — commit sonrası doldurulur)

```
(commit + push sonrası)
```

---

## 15. Sonraki adım

1. **L5 geliştirici:** F16.2.9 raporu + commit onayı.
2. Sırada **F16.2.4 Blog** / **F16.2.5 Şehirler** / **F16.2.6 Sertifikalar** / **F16.2.7 Ekip** / **F16.2.10 Ayarlar** — geliştirici önceliği (girdi bekleyen satırlar).
3. F16.2.11 kapanışta: tüm modül sayım + tam `/denetle` + seo-analyzer + F16.2 `tamamlandı`.
